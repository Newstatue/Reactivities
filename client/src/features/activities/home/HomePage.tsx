import { Container, Typography } from "@mui/material";

export default function HomePage() {
    return (
        <Container sx={{ mt: 3 }}>
            <Typography fontSize="1rem">Home Page</Typography>
        </Container>
    )
}